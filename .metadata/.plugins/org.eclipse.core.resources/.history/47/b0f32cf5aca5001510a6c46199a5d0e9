package br.com.dup.money;

import java.util.ArrayList;

public class CountMoney {
	private int value;
	private int count;
	private int array[] = {100, 50, 20, 10, 5, 1};
	private String str;

	public CountMoney(int val){
		this.value = val;
		this.count = 1;
		this.setStr("");
	}

	public void run(){
		while(this.value != 0)
			this.cedulas();
	}

	private boolean cedulas(){		
		for(int k : this.array){
			if(k <= this.value){
				this.setStr(this.getStr() + "valor: "+this.value+" ---- "+this.count+"-> cedula: R$"+k+"<br>");
				this.value -= k;
				this.count++;
				return true;
			}
		}
		return false;
	}

	public String getStr() {
		return str;
	}
}
